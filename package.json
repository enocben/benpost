{
  "name": "benpost",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun --filter back dev\" \"bun --filter front dev\"",
    "build": "bun --filter back run build && bun --filter front run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
